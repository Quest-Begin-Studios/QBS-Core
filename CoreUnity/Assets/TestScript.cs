using UnityEngine;
using QBS.Core;

public class TestScript : MonoBehaviour
{
    // TryLogAH tryGetScript;
    // // Start is called once before the first execution of Update after the MonoBehaviour is created
    // void Start()
    // {
    //     tryGetScript = new TryLogAH();
    //     tryGetScript.Log(21, 34);
    // }
}
